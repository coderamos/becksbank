import React from 'react';

import * as s from './styles';

const Header: React.FC = () => {
  return (
    <s.HeaderContainer>
      <span>HEADER</span>
    </s.HeaderContainer>
  );
};

export default Header;
